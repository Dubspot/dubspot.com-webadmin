<?php

class Basket_itemsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('basket_items')->truncate();

		$basket_items = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('basket_items')->insert($basket_items);
	}

}
