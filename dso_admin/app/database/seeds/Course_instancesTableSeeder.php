<?php

class Course_instancesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('course_instances')->truncate();

		$course_instances = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('course_instances')->insert($course_instances);
	}

}
