<?php

class Scheduled_paymentsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('scheduled_payments')->truncate();

		$scheduled_payments = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('scheduled_payments')->insert($scheduled_payments);
	}

}
