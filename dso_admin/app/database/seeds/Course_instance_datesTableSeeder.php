<?php

class Course_instance_datesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('course_instance_dates')->truncate();

		$course_instance_dates = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('course_instance_dates')->insert($course_instance_dates);
	}

}
