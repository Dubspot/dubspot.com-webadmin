<?php

class Program_instancesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('program_instances')->truncate();

		$program_instances = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('program_instances')->insert($program_instances);
	}

}
