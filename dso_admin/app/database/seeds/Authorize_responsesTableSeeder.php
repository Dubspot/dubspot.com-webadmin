<?php

class Authorize_responsesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('authorize_responses')->truncate();

		$authorize_responses = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('authorize_responses')->insert($authorize_responses);
	}

}
