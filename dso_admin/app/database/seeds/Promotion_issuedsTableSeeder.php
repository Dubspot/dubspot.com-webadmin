<?php

class Promotions_issuedTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('promotions_issued')->truncate();

		$promotions_issued = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('promotions_issued')->insert($promotions_issued);
	}

}
